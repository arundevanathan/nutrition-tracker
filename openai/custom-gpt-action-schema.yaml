openapi: 3.1.0
info:
  title: Nutrition Tracker GPT Actions
  version: 0.2.0
  description: MVP Custom GPT Actions for authenticated nutrition logging and dashboard retrieval.

servers:
  - url: https://app.calorie-track.com

paths:
  /api/me:
    get:
      operationId: getAuthenticatedUser
      summary: Get authenticated user details
      description: Returns the nutrition profile associated with the linked Custom GPT account.
      security:
        - OAuth2: []
      responses:
        "200":
          description: Authenticated user details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthenticatedUser"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/dashboard:
    get:
      operationId: getDashboard
      summary: Get dashboard data
      description: Returns today, yesterday, last 14 days, recent entries, and lightweight trend insights for the authenticated user.
      security:
        - OAuth2: []
      responses:
        "200":
          description: Dashboard data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Dashboard"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/food-entry:
    post:
      operationId: createFoodEntry
      summary: Log a food entry
      description: Creates a food entry owned by the authenticated bearer token user. Do not send user_id.
      security:
        - OAuth2: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateFoodEntryRequest"
      responses:
        "201":
          description: Created food entry
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateFoodEntryResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/food-entry/{id}:
    patch:
      operationId: updateFoodEntry
      summary: Update a food entry
      description: Updates an authenticated user's existing food entry. Use an id from a previous log response or dashboard recent entries. Do not send user_id.
      security:
        - OAuth2: []
      parameters:
        - name: id
          in: path
          required: true
          description: Food entry id returned by createFoodEntry or getDashboard.
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateFoodEntryRequest"
      responses:
        "200":
          description: Updated food entry
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateFoodEntryResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: Food entry not found for the authenticated user

    delete:
      operationId: deleteFoodEntry
      summary: Delete a food entry
      description: Deletes an authenticated user's existing food entry. Use an id from a previous log response or dashboard recent entries.
      security:
        - OAuth2: []
      parameters:
        - name: id
          in: path
          required: true
          description: Food entry id returned by createFoodEntry or getDashboard.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Deleted food entry
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeleteFoodEntryResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: Food entry not found for the authenticated user

  /api/weight-entry:
    post:
      operationId: createWeightEntry
      summary: Log a weight entry
      description: Creates or updates the authenticated user's weight entry for a date. Do not send user_id.
      security:
        - OAuth2: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateWeightEntryRequest"
      responses:
        "201":
          description: Saved weight entry
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateWeightEntryResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/weight-entry/{id}:
    patch:
      operationId: updateWeightEntry
      summary: Update a weight entry
      description: Updates an authenticated user's existing weight entry. Use an id from getDashboard recent_weight_entries. Do not send user_id.
      security:
        - OAuth2: []
      parameters:
        - name: id
          in: path
          required: true
          description: Weight entry id returned by getDashboard.
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateWeightEntryRequest"
      responses:
        "200":
          description: Updated weight entry
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateWeightEntryResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: Weight entry not found for the authenticated user

    delete:
      operationId: deleteWeightEntry
      summary: Delete a weight entry
      description: Deletes an authenticated user's existing weight entry. Use an id from getDashboard recent_weight_entries.
      security:
        - OAuth2: []
      parameters:
        - name: id
          in: path
          required: true
          description: Weight entry id returned by getDashboard.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Deleted weight entry
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeleteWeightEntryResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: Weight entry not found for the authenticated user

  /api/delete-all-data:
    post:
      operationId: deleteAllUserData
      summary: Delete all nutrition tracking data
      description: Deletes all food entries, weight entries, daily summaries, and API logs for the authenticated user. Requires exact confirmation text.
      security:
        - OAuth2: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DeleteAllUserDataRequest"
      responses:
        "200":
          description: Deleted all nutrition tracking data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeleteAllUserDataResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"

components:
  responses:
    Unauthorized:
      description: Missing, invalid, revoked, or expired bearer token
    ValidationError:
      description: Request validation failed
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ValidationError"

  schemas:
    AuthenticatedUser:
      type: object
      required:
        - user_id
        - email
        - timezone
      properties:
        user_id:
          type: string
          format: uuid
        email:
          type: string
          format: email
        display_name:
          type:
            - string
            - "null"
        timezone:
          type: string

    CreateFoodEntryRequest:
      type: object
      additionalProperties: false
      required:
        - description
        - consumption_date
        - calories
      properties:
        description:
          type: string
          minLength: 1
        consumption_date:
          type: string
          pattern: "^\\d{4}-\\d{2}-\\d{2}$"
        consumption_time:
          type: string
          pattern: "^\\d{2}:\\d{2}(:\\d{2})?$"
        meal_type:
          type: string
          enum:
            - breakfast
            - lunch
            - dinner
            - snack
            - drink
            - other
        entry_type:
          type: string
          enum:
            - Core
            - Junk
            - Alcohol
            - Eating Out
          default: Core
        calories:
          type: integer
          minimum: 0
        protein_g:
          type: number
          minimum: 0
          default: 0
        carbs_g:
          type: number
          minimum: 0
          default: 0
        fat_g:
          type: number
          minimum: 0
          default: 0
        confidence:
          type: string
          enum:
            - high
            - medium
            - low
        notes:
          type: string

    CreateFoodEntryResponse:
      type: object
      required:
        - food_entry
        - summary
        - today
      properties:
        food_entry:
          $ref: "#/components/schemas/FoodEntry"
        summary:
          type: string
        today:
          $ref: "#/components/schemas/TodaySnapshot"

    UpdateFoodEntryRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        description:
          type: string
          minLength: 1
        consumption_date:
          type: string
          pattern: "^\\d{4}-\\d{2}-\\d{2}$"
        consumption_time:
          type:
            - string
            - "null"
          pattern: "^\\d{2}:\\d{2}(:\\d{2})?$"
        meal_type:
          type:
            - string
            - "null"
          enum:
            - breakfast
            - lunch
            - dinner
            - snack
            - drink
            - other
            - null
        entry_type:
          type: string
          enum:
            - Core
            - Junk
            - Alcohol
            - Eating Out
        calories:
          type: integer
          minimum: 0
        protein_g:
          type: number
          minimum: 0
        carbs_g:
          type: number
          minimum: 0
        fat_g:
          type: number
          minimum: 0
        confidence:
          type:
            - string
            - "null"
          enum:
            - high
            - medium
            - low
            - null
        notes:
          type:
            - string
            - "null"

    UpdateFoodEntryResponse:
      type: object
      required:
        - food_entry
        - summary
        - today
      properties:
        food_entry:
          $ref: "#/components/schemas/FoodEntry"
        summary:
          type: string
        today:
          $ref: "#/components/schemas/TodaySnapshot"

    DeleteFoodEntryResponse:
      type: object
      required:
        - deleted_food_entry
        - summary
        - today
      properties:
        deleted_food_entry:
          $ref: "#/components/schemas/FoodEntry"
        summary:
          type: string
        today:
          $ref: "#/components/schemas/TodaySnapshot"

    CreateWeightEntryRequest:
      type: object
      additionalProperties: false
      required:
        - date
        - weight_kg
      properties:
        date:
          type: string
          pattern: "^\\d{4}-\\d{2}-\\d{2}$"
        weight_kg:
          type: number
          exclusiveMinimum: 0
        note:
          type: string

    CreateWeightEntryResponse:
      type: object
      required:
        - weight_entry
        - summary
      properties:
        weight_entry:
          $ref: "#/components/schemas/WeightEntry"
        summary:
          type: string

    UpdateWeightEntryRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        date:
          type: string
          pattern: "^\\d{4}-\\d{2}-\\d{2}$"
        weight_kg:
          type: number
          exclusiveMinimum: 0
        note:
          type:
            - string
            - "null"

    UpdateWeightEntryResponse:
      type: object
      required:
        - weight_entry
        - summary
      properties:
        weight_entry:
          $ref: "#/components/schemas/WeightEntry"
        summary:
          type: string

    DeleteWeightEntryResponse:
      type: object
      required:
        - deleted_weight_entry
        - summary
      properties:
        deleted_weight_entry:
          $ref: "#/components/schemas/WeightEntry"
        summary:
          type: string

    DeleteAllUserDataRequest:
      type: object
      additionalProperties: false
      required:
        - confirmation
      properties:
        confirmation:
          type: string
          enum:
            - DELETE ALL MY DATA

    DeleteAllUserDataResponse:
      type: object
      required:
        - summary
        - deleted
      properties:
        summary:
          type: string
        deleted:
          type: object
          required:
            - food_entries
            - weight_entries
            - daily_summaries
            - api_logs
          properties:
            food_entries:
              type: integer
            weight_entries:
              type: integer
            daily_summaries:
              type: integer
            api_logs:
              type: integer

    Dashboard:
      type: object
      required:
        - user
        - today
        - yesterday
        - last_14_days
        - recent_food_entries
        - recent_weight_entries
        - metadata
        - insights
      properties:
        user:
          $ref: "#/components/schemas/AuthenticatedUser"
        today:
          type: object
          required:
            - date
            - totals
            - food_entries
            - latest_weight
          properties:
            date:
              type: string
            totals:
              $ref: "#/components/schemas/DailyTotals"
            food_entries:
              type: array
              description: All food entries logged for this dashboard date.
              items:
                $ref: "#/components/schemas/FoodEntry"
            latest_weight:
              anyOf:
                - $ref: "#/components/schemas/WeightEntry"
                - type: "null"
        yesterday:
          type: object
          required:
            - date
            - totals
            - food_entries
          properties:
            date:
              type: string
            totals:
              $ref: "#/components/schemas/DailyTotals"
            food_entries:
              type: array
              description: All food entries logged for yesterday.
              items:
                $ref: "#/components/schemas/FoodEntry"
        last_14_days:
          type: object
          required:
            - start_date
            - end_date
            - days
            - averages
          properties:
            start_date:
              type: string
            end_date:
              type: string
            days:
              type: array
              items:
                $ref: "#/components/schemas/DailyTotals"
            averages:
              $ref: "#/components/schemas/DailyAverages"
        recent_food_entries:
          type: array
          items:
            $ref: "#/components/schemas/FoodEntry"
        recent_weight_entries:
          type: array
          items:
            $ref: "#/components/schemas/WeightEntry"
        metadata:
          type: object
          required:
            - generated_at
            - timezone
            - recent_limit
          properties:
            generated_at:
              type: string
              format: date-time
            timezone:
              type: string
            recent_limit:
              type: integer
            days_included:
              type: integer
        insights:
          $ref: "#/components/schemas/DashboardInsights"

    TodaySnapshot:
      type: object
      required:
        - date
        - totals
      properties:
        date:
          type: string
        totals:
          $ref: "#/components/schemas/DailyTotals"

    FoodEntry:
      type: object
      required:
        - id
        - logged_at
        - consumption_date
        - entry_type
        - description
        - calories
        - protein_g
        - carbs_g
        - fat_g
        - source
      properties:
        id:
          type: string
          format: uuid
        logged_at:
          type: string
          format: date-time
        consumption_date:
          type: string
        consumption_time:
          type:
            - string
            - "null"
        meal_type:
          type:
            - string
            - "null"
        entry_type:
          type: string
        description:
          type: string
        calories:
          type: integer
        protein_g:
          type: number
        carbs_g:
          type: number
        fat_g:
          type: number
        confidence:
          type:
            - string
            - "null"
        source:
          type: string
        notes:
          type:
            - string
            - "null"

    WeightEntry:
      type: object
      required:
        - id
        - date
        - weight_kg
      properties:
        id:
          type: string
          format: uuid
        date:
          type: string
        weight_kg:
          type: number
        note:
          type:
            - string
            - "null"

    DailyTotals:
      type: object
      required:
        - date
        - calories
        - protein_g
        - carbs_g
        - fat_g
        - junk_calories
        - alcohol_calories
        - eating_out_calories
        - entries_count
      properties:
        date:
          type: string
        calories:
          type: integer
        protein_g:
          type: number
        carbs_g:
          type: number
        fat_g:
          type: number
        junk_calories:
          type: integer
        alcohol_calories:
          type: integer
        eating_out_calories:
          type: integer
        entries_count:
          type: integer

    DailyAverages:
      type: object
      required:
        - calories
        - protein_g
        - carbs_g
        - fat_g
        - junk_calories
        - alcohol_calories
        - eating_out_calories
        - entries_count
      properties:
        calories:
          type: integer
        protein_g:
          type: number
        carbs_g:
          type: number
        fat_g:
          type: number
        junk_calories:
          type: integer
        alcohol_calories:
          type: integer
        eating_out_calories:
          type: integer
        entries_count:
          type: number

    DashboardInsights:
      type: object
      required:
        - days_logged
        - highest_calorie_day
        - lowest_calorie_day
        - logged_days_last_7_days
        - logged_days_previous_7_days
        - avg_calories_last_7_days
        - avg_calories_previous_7_days
        - calorie_trend_delta
        - avg_protein_last_7_days
        - avg_protein_previous_7_days
        - protein_trend_delta
        - weight_latest
        - weight_previous
        - weight_delta
      properties:
        days_logged:
          type: integer
        highest_calorie_day:
          anyOf:
            - $ref: "#/components/schemas/DailyTotals"
            - type: "null"
        lowest_calorie_day:
          anyOf:
            - $ref: "#/components/schemas/DailyTotals"
            - type: "null"
        logged_days_last_7_days:
          type: integer
        logged_days_previous_7_days:
          type: integer
        avg_calories_last_7_days:
          type: integer
        avg_calories_previous_7_days:
          type: integer
        calorie_trend_delta:
          type: integer
        avg_protein_last_7_days:
          type: number
        avg_protein_previous_7_days:
          type: number
        protein_trend_delta:
          type: number
        weight_latest:
          anyOf:
            - $ref: "#/components/schemas/WeightEntry"
            - type: "null"
        weight_previous:
          anyOf:
            - $ref: "#/components/schemas/WeightEntry"
            - type: "null"
        weight_delta:
          type:
            - number
            - "null"

    ValidationError:
      type: object
      required:
        - error
        - message
        - fields
      properties:
        error:
          type: string
          const: validation_error
        message:
          type: string
        fields:
          type: object
          additionalProperties:
            type: string

  securitySchemes:
    OAuth2:
      type: oauth2
      flows:
        authorizationCode:
          authorizationUrl: https://app.calorie-track.com/oauth/authorize
          tokenUrl: https://app.calorie-track.com/oauth/token
          scopes: {}
